# Copy to .env.local and fill in values. These are optional for local dev.
# When omitted, the app will run with Auth disabled (shows a friendly notice).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
